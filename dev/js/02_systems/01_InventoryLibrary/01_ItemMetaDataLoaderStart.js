/* =============================
= ITEM METADATA LOADER - START =
============================= */
if (typeof setup === 'undefined') {
	setup = {};
}
if (typeof setup.ItemData === 'undefined') {
	setup.ItemData = {};
}